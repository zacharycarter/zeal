
template builtin = discard

proc newGame(mapDir, mapName: string) = builtin
proc setAmbientLightColor(val: array[3, float32]) = builtin
proc setEmitLightColor(val: array[3, float32]) = builtin
proc setEmitLightPos(val: array[3, float32]) = builtin
